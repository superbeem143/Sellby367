import React from 'react'

export default function Saved(){
  return (
    <div>
      <h2 className="text-xl font-semibold">Saved Listings</h2>
      <p className="mt-4 text-gray-500">You can save listings to view later. (Feature: saved listings stored per-user in Firestore — integrate as needed.)</p>
    </div>
  )
}
